# Data Directory

This directory stores uploaded documents and processed data.

**Note**: This directory is ignored by git for security reasons. Uploaded documents are not committed to the repository.

## Structure

- `uploads/`: Temporarily stored uploaded files
- `processed/`: Processed document chunks and metadata
- `backups/`: Optional backup storage

## Security

- All files in this directory are ignored by git
- Ensure sensitive documents are handled appropriately
- Consider implementing additional encryption for sensitive data
